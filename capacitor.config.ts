import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.listening.project',
  appName: 'Listening Project',
  webDir: 'dist'
};

export default config;
